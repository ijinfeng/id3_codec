name: id3_codec
description: A cross-platform, pure-Dart package for reading and editing ID3 meta data.
version: 1.0.3
homepage: https://github.com/ijinfeng/flutter_id3

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

